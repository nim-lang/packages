os:
  - linux
dist: trusty

language: c

cache:
  directories:
    - "$HOME/.choosenim"

install:
  - export CHOOSENIM_CHOOSE_VERSION="1.2.0"
  - |
      curl https://nim-lang.org/choosenim/init.sh -sSf > init.sh
      sh init.sh -y
  - export PATH=$HOME/.nimble/bin:$PATH

before_script:
  - set -e
  - set -x

script:
  - nim -v
  - nim -v
  - nim --aasdf1
  - nim -v
  - nim --aasdf2
  - nim -v
  - nim js -r validate_json.nim
  - nim c -d:ssl -r package_scanner.nim
  - nim -v

notifications:
  email: false # noisy
